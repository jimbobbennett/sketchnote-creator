node_modules/
scripts/node_modules/
*.log
.DS_Store

# Per-video working dirs / generated media (kept out of the repo)
work/
out/
research/nitya-samples/

# Generated per-video assets (outputs, not source).
assets/

# Design systems: only the generic default + README are committed. Drop your own brand in
# design/<name>/ and it stays local (e.g. design/arize/).
design/*
!design/default/
!design/README.md

# Committed sample outputs live in examples/ (not ignored)
